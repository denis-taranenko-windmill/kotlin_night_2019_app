name: kotlin_night_2019_app
description: A new Flutter application for Kotlin Night 2019.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+2
  provider: ^3.0.0
  rxdart: ^0.22.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
